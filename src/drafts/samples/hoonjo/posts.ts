import type { Metric } from './components';
import mojImg from './assets/moj-portal.png';
import kistiImg from './assets/kisti-ml.png';
import solvookImg from './assets/solvook-expert.png';

/* Works archive — the long-form write-ups behind the headline projects.
   The main page features a concise project card; the full story lives here.
   Bodies are markdown (rendered with react-markdown). `slug` matches the
   case `id` so a project card links to /works/<slug>. */

export type Post = {
  slug: string;
  title: string;
  company: string;
  period: string;
  role: string;
  summary: string;
  tags: string[];
  metrics?: Metric[];
  hero?: { src: string; alt: string };
  repo?: { label: string; href: string };
  body: string;
};

export const posts: Post[] = [
  {
    slug: 'work-column-pager',
    title: '사내 페이지네이션 엔진을 독립 오픈소스로',
    company: '@Bookips',
    period: '2025 — 2026',
    role: 'Frontend · 단독 설계/개발',
    summary: '긴 글이든 카드든, 어떤 React 화면이든 인쇄물처럼 “고정 크기 페이지”로 자동으로 나눠주는 엔진. 같은 문제를 세 번 풀며 도달했다.',
    tags: ['TypeScript', 'React 18/19', 'Vite', 'Vitest', 'semantic-release'],
    metrics: [
      { label: '100장 순서 재배치', after: '55–66', unit: 'ms', gain: '변경 없는 항목은 재측정 생략' },
      { label: '편집 1건 반영', after: '11–16', unit: 'ms', gain: '동기 측정으로 즉시 반영' },
      { label: '결정적 테스트', after: '49', unit: '개', gain: '측정부 교체 가능하게 추상화' },
    ],
    repo: { label: 'GitHub · H8njo/column-pager', href: 'https://github.com/H8njo/column-pager' },
    body: `
## 문제

교육 콘텐츠 편집기에서 문제들을 A4 두 칸(2단) 레이아웃으로 배치해야 했다. 기존 구현은 일반 카드 높이를 측정해 두 열에 배치하는 방식이었는데, **한 칸 높이를 넘는 긴 카드(긴 본문)를 처리하지 못했다.** 이게 제일 치명적이었다.

문제지는 인쇄돼 학생에게 가는 거라, 문장이 중간에 잘리면 그대로 불량품이 된다. 그래서 주력 서비스에서 **2년 가까이 환불 문의가 이어졌다.** 여러 사람이 붙어 봤지만, 다들 같은 벽에서 멈췄다.

> CSS 자동 다단으로 안 푼 이유도 있다. 카드마다 호버·선택 같은 인터랙션이 붙어야 했고, 서버에서 같은 HTML을 PDF로 변환하는데 CSS 다단은 화면이랑 PDF가 달라졌다.

## 다섯 번의 시도

1. **긴 카드만 이미지로 캡처해 자르기.** 픽셀로 잘라버리니 글자가 중간에 뭉텅 잘렸다. 줄 간격(여백)을 감지해 안전한 지점에서 끊으려 했지만 이미지 연산이 너무 무거웠다.
2. **\`column-count\` CSS에 통째로 맡기기.** 다음 페이지로 넘길 방법이 없어 페이지마다 전체를 렌더 → 카드 1,000개 × 10페이지 = 1만 개 렌더로 화면이 그대로 블락됐다. 청크·이중 \`requestAnimationFrame\`·\`flushSync\`까지 동원했지만 이번엔 페이지 넘김이 눈에 띄게 느려졌다.
3. **다시 높이 측정 + 잘린 지점 직접 탐지.** 글을 inline-block으로 쪼개 overflow 지점을 이진 탐색으로 찾았는데도 부담이 컸고, 카드 padding 탓에 “진짜 마지막 글자”를 정확히 못 짚었다.
4. **1번의 줄 자르기를 2번으로 메우기.** 긴 카드가 있는 페이지에만 \`column-count\`를 적용. 그런데 짧은 카드까지 다음 페이지에 복제해야 해, 페이지가 늘수록 비효율적이었다.
5. **카드마다 \`column-count:1\` + 클립 + 복제 — 정답.** 짧은 카드는 그대로. 긴 카드는 \`column-count:1\` + \`overflow:hidden\`이라, 공간이 모자라면 내용이 옆으로 흐르는 특성을 그대로 쓴다. 잘린 조각만 다음 열에 복제하고 \`translateX\`를 컬럼 폭만큼 옮기면 이어지듯 보인다.

> **핵심 — 실패를 버리지 않고 합치다.** 새로 발명한 게 아니라, 1번이 실패한 “자연스러운 줄 자르기”를 2번의 \`column-count\`가 공짜로 해준다는 걸, 카드 단위 크롭 도구로만 끌어 썼다. 망한 시도들을 버린 게 아니라 각도만 바꿔 끼워 맞췄다.

## 세 세대에 걸쳐

- **v1 (솔북 문제 에디터)** — 위 다섯 번을 다 거쳐 도메인에 박힌 채 동작한 원형.
- **v2 (솔북 다른 제품)** — 임의 콘텐츠로 일반화. 핵심 훅이 210줄까지 비대해진, 손대기 무서운 코드.
- **v3 (독립 npm \`column-pager\`)** — 앱에서 떼어내 순수 코어 / 측정 어댑터 / React 렌더 3계층으로 재설계. 검증된 수학(크롭·측정)은 그대로 이식하고 구조만 새로 짰다. 백지에서 다시 짜면 회귀가 쏟아지니까.

서브픽셀에서도 한 번 데였다. \`2090 / 697 = 2.998\`인데 실제론 3컬럼이라, \`Math.floor\`면 2로 내려가 마지막 컬럼이 통째로 사라진다. DOM 측정값은 폰트 로드나 줌 때문에 정수배에서 늘 살짝 빗나가서, “정수 N배여야 하는” 값엔 \`round\`를 쓴다.

## 결과

지금은 라이브러리 없이 임의의 React 콘텐츠를 고정 높이 페이지로 나누는 오픈소스 패키지가 됐다(\`semantic-release\`로 main 머지 = 자동 publish). 이 사이트의 라이브 데모도 그 패키지로 돈다.

제품 효과가 더 컸다. 매출 비중이 큰 주력 서비스에서 **2년 가까이 이어지던 환불 문의가 빠졌다.** 재사용성·확장성을 챙긴 덕에 후속 저작 도구, 나아가 AI 문제 생성 서비스(현 메인 매출의 한 축)로 이어졌고, 그즈음 매출이 전년의 약 150%가 됐다.

## 회고

잘한 건 새로운 걸 발명한 게 아니라, 망한 시도를 안 버리고 각도만 바꿔 다시 쓴 거였다. 1번에서 “픽셀로 자르면 글자가 깨진다”를 직접 당하지 않았으면 줄 자르기를 \`column-count\`에 맡길 생각을 못 했고, 2번에서 “전체를 다 렌더하면 죽는다”를 안 겪었으면 “잘린 카드만 복제하자”까지 못 갔다. 그 실패들을 건너뛸 방법은 애초에 없었다. 못 푼 한계(표 분할, 페이지 넘는 카드의 미세한 끊김)는 문서에 그대로 적어뒀다.
`.trim(),
  },
  {
    slug: 'work-pdf',
    title: '300페이지 PDF, 첫 화면을 10분에서 1초로',
    company: '@Sling',
    period: '2023 — 2024',
    role: 'Frontend · PDF/메모리 도메인',
    summary: '교재 PDF 뷰어가 모든 페이지를 다 그릴 때까지 멈춰 있던 걸, “필요한 것부터 먼저”로 바꿔 첫 조작을 약 1초로 만들었다.',
    tags: ['브라우저 PDF', 'Canvas', '청크 렌더', '메모리', 'OpenCV'],
    metrics: [
      { label: '첫 조작까지 · TTI 300p', before: '639,000ms', after: '1,310', unit: 'ms', gain: '약 488배 단축' },
      { label: '초기 로딩 · 페이지당', before: '2,132ms', after: '1,527', unit: 'ms', gain: '비례 구조 제거' },
      { label: '대용량 작업 속도', after: '약 4', unit: '× 빠름', gain: '청크 + 즉시 해제' },
    ],
    body: `
## 문제

300페이지 교재 PDF를 열면 모든 페이지가 다 그려질 때까지 화면이 멈춰 있었다. 페이지 한 장 그리는 데 약 2초(2,132ms), 300장이면 단순히 곱해도 10분이 넘는다. 실제로 첫 조작이 가능해지기까지(TTI) **약 639,000ms — 10분 30초.** 작업자는 그동안 아무것도 못 하고 기다려야 했다.

이건 서버에서 PDF를 굽는 일이 아니라 브라우저 쪽 PDF 뷰어였다. 각 페이지를 canvas로 래스터해 그 위에서 문제 영역을 잡아 잘라내는 도구인데, 초기 구조는 **모든 페이지를 미리 이미지로 렌더해 통째로 state에 들고**, 그게 다 끝나야 화면을 열어줬다. 거기에 각 이미지가 \`scale = 3 × dpr\`로 키운 거대한 base64라, 수백 장을 동시에 들면 대용량에서 탭이 그대로 얼었다.

## 전체를 기다리지 말고, 보이는 것부터

전체 렌더가 끝날 때까지 기다리는 구조 자체를 버렸다.

- 페이지를 슬라이더로 골라 보는 UI로 바꾸고, 렌더링은 앞단에서 끝내는 대신 **백그라운드로** 돌렸다. 2페이지씩 청크로 잘라 순차 처리하니 메인 스레드가 안 막혔다.
- **우선순위가 핵심.** 슬라이더로 포커싱된 페이지를 먼저 그리고, 그게 끝나면 나머지를 뒤에서 채운다. 사용자가 보는 페이지가 항상 먼저 나온다.
- 로딩 중에도 조작을 막지 않았다. 안 그려진 페이지를 건드리면 그 자리에서 즉석 렌더해 바로 보여줬다.

\`\`\`ts
const makeImage = async (pdf, n) => {
  const page = await pdf.getPage(n);
  const img = await getViewportImage(page, scale ?? devicePixelRatio, true);
  page.cleanup();                 // 래스터 직후 pdf.js 리소스 해제 (메모리 누수 차단)
  return img;
};
for (const c of chunk(pageNumbers, 2)) {
  const results = await Promise.all(c.map((n) => makeImage(pdf, n)));
  setPageImages((prev) => [...prev, ...results]); // 점진 push → 진행률
}
\`\`\`

청크로 나눠 그리면서 \`page.cleanup()\`으로 바로 버리니, peak 메모리도 전체 페이지 수가 아니라 **청크 크기에 묶였다.** 블락이 풀린 김에 탭이 죽던 것도 같이 잡혔다.

## 결과

- 첫 화면: 300페이지 기준 639,000ms(약 10분 30초) → **1,310ms(약 1초). 약 488배.** 초기 로딩이 페이지 수에 비례하던 구조 자체가 사라졌다.
- UX: 로딩 중에도 인터랙션을 열어둬, 다 그려지길 기다리는 단계가 없어졌다.
- 작업 속도: 대용량 PDF에서 이미지 처리 작업이 약 4배 빨라졌다.
- 메모리: peak가 청크 크기로 고정돼, 대용량 교재에서도 탭이 죽지 않는다.

## 회고

결국 “한 번에 다 하려다 멈춘다”는 얘기였다. 전 페이지를 동시에 그려서 첫 화면이 밀렸고, 동시에 메모리에 쥐어서 탭이 얼었고. 둘 다 “한 번에 다”를 “지금 필요한 것부터, 나머지는 뒤에서”로 바꾸는 일이었다.

습관도 하나 남았다. **메모리를 줄였으면 그 자리에서 결과물부터 다시 본다.** 자동 영역 선택의 렌더 품질을 \`0.2\`로 내렸을 때 OpenCV가 조용히 엉뚱한 경계를 잡고 있던 걸 한참 못 봤다. 메모리는 숫자로 바로 보이는데 정확도는 결과를 들여다봐야 보이니까. 한 번 당하고 배웠다.
`.trim(),
  },
  {
    slug: 'work-portal',
    title: '컬럼 정의 한 벌로 CRUD 화면 59개를 찍어낸 보안 포털',
    company: '@Zipida · 법무부',
    period: '2021 — 2023',
    role: 'Frontend 주저자 + Backend 상당 부분',
    summary: '같은 모양 화면을 100번 만들 순 없었다. 그래서 화면을 컬럼 배열 하나로 줄였다.',
    tags: ['React', 'GraphQL', 'Strapi', 'RBAC', 'Fullstack'],
    hero: { src: mojImg, alt: '법무부 보안관제 포털 화면' },
    metrics: [
      { label: '화면 양산 · 1벌 정의', after: '59', unit: '개', gain: '두 명이 도메인 100여 개' },
      { label: '권한 게이트', after: '라우트 1벌', gain: '메뉴·권한·체크키 동시 파생' },
      { label: '내 커밋 · 프론트 주저자', after: '696', unit: '/1,299', gain: '약 2년' },
    ],
    body: `
## 문제

정부 기관이 쓰는 관리자 포털(보안 관제 시스템)을 두 명이 만들었다. 도메인은 100개가 넘었다. 자산 목록, 취약점 진단 결과, 블랙리스트, 통보 이력… 화면마다 도메인은 다른데 생긴 건 다 똑같다. **“목록 + 검색 + 정렬 + 엑셀 + 추가·수정·삭제 모달”.** 이걸 100번 손으로 짤 수는 없었다. 같은 모양 화면 100개는 같은 버그를 100군데 심는다는 뜻이기도 하다.

이 화면들이 다른 건 결국 “어떤 컬럼을 보여주느냐”뿐이다. 그런데 **컬럼 하나가 다섯 군데서 서로 다르게 해석돼야 했다.** 같은 \`status\` 컬럼이 표에선 색깔 뱃지, 검색에선 select, 엑셀에선 코드값을 한글 라벨로, 폼에선 입력칸, 어떤 화면에선 아예 폼에서만 숨김. **하나의 정의, 다섯 개의 얼굴.**

## 결정 — 추상화의 누수를 각오하고 키웠다

화면마다 컴포넌트를 따로 두고 명시적으로 가느냐, 컬럼 메타 하나로 다 찍어내고 누수를 감수하느냐. 두 명이 도메인 100개를 감당하는 상황에선 답이 정해져 있었다. 후자. 대신 누수는 각오했다 — 표에선 맞는데 엑셀 enum에서만 깨지는 케이스가 반드시 나오고, 실제로도 그랬다(\`//TODO: valueEnum 문제로 인한 임시 방편\` 주석으로 우회로를 뚫었다). **추상화의 힘과 누수가 한 몸이라는 걸 제대로 배웠다.**

## 접근

컬럼 객체 하나에 플래그를 달아 다섯 갈래로 흘려보냈다. \`searchDefine\`이면 검색 입력, \`export:false\`면 엑셀 제외, \`editable:false\`면 폼에서 disabled. 표 한 열로 시작한 정의가 검색·정렬·엑셀·모달폼까지 전부 만든다. 이 \`Table\` 컴포넌트를 결국 \`pages/\` 아래 **59개 파일이 import**했다.

권한도 컴포넌트가 스스로 판단하게 했다. 권한 목록을 따로 테이블로 관리하면 메뉴가 바뀔 때마다 어긋난다. 그래서 **라우트를 권한의 원천으로** 삼았다 — 라우트 정의 한 벌에서 메뉴 트리·권한 설정 트리·런타임 체크 키가 동시에 나온다. 권한 키는 그냥 \`경로/액션\`(\`/policy/blacklist/UPDATE\`). 메뉴를 추가하면 권한이 자동으로 따라온다.

\`\`\`jsx
// Table/index.jsx — 버튼은 권한이 있을 때만
const permissionCreate = user.hasPermission(\`\${path || pathname}/CREATE\`);
\`\`\`

## 결과

같은 추상화를 쓴다는 건 **한 번 잡은 버그가 59개 화면에서 한꺼번에 고쳐진다**는 뜻이기도 했다. 제일 끈질겼던 페이지 리셋 버그(검색·정렬 시 page를 1로 안 돌려 직전 offset이 살아남던)도, 핸들러마다 사람이 기억해야 하는 계약을 없애고 입력이 바뀌는 모든 경로가 같은 리셋을 통과하게 추상화 한 곳에 강제했다. **빠뜨리기 쉬운 계약일수록 사람한테 맡기면 안 된다.**

화면 59개, 도메인 100여 개를 두 명이 굴렸다. 그리고 이 \`Table\` 컴포넌트는 이후 다른 프로젝트들에서도 컬럼 배열만 갈아끼워 재사용됐다.

## 회고

이 프로젝트는 처음부터 끝까지 “정의를 어디 한 군데로 모으느냐” 싸움이었다. 화면의 정의는 컬럼 배열로, 권한의 정의는 라우트로, 페이지 리셋은 훅으로. **흩어진 진실을 한 곳으로 끌어모으면 버그도 한 곳에서 잡힌다. 대신 그 한 곳이 무너지면 59개가 같이 무너진다.** 그 무게를 견디는 게 추상화를 만드는 사람의 몫이라는 걸 여기서 배웠다.
`.trim(),
  },
  {
    slug: 'work-design-system',
    title: '아이콘 드롭하면 타입까지, 디자인 시스템 자동화',
    company: '@Bookips',
    period: '2024 — 현재',
    role: 'Frontend · 코드오너 / 릴리스',
    summary: '컴포넌트를 잘 만드는 거랑 디자인 시스템을 만드는 건 다른 일이었다. 그 사이에 빌드가 두 번 죽었다.',
    tags: ['Radix', 'Tailwind · CVA', '코드젠', 'Storybook', 'Rollup/Vite'],
    hero: { src: solvookImg, alt: '디자인 시스템이 적용된 솔북 제품 화면' },
    metrics: [
      { label: 'multi-select', after: '일급 모델', gain: '우회 → 체크박스·aria 정상' },
      { label: '아이콘 추가', after: '폴더 드롭', gain: '손으로 쓰던 export 0' },
      { label: '역할', after: '코드오너', gain: '모든 PR 리뷰·머지·릴리스' },
    ],
    body: `
## 태도 — 기본 컴포넌트는 마법보다 예측 가능

여러 프론트엔드 제품이 같은 UI·접근성을 공유해야 했다. React 컴포넌트 라이브러리를 Radix primitive + Tailwind + CVA 위에 올리고 Storybook으로 문서화해 사내 패키지로 배포하는 구조다.

그 전에 태도 얘기 하나. Button 초기 버전에 욕심을 부려서, async \`onClick\`이 promise를 반환하면 가로채 로딩 상태를 알아서 켜고 꺼주게 만들었다. 데모로는 그럴듯했는데 **기본 컴포넌트로는 그냥 틀린 설계였다 — 상태 주도권을 호출자한테서 뺏어온다.** controlled \`isLoading\`으로 되돌렸다. 기본 컴포넌트에서는 마법보다 예측 가능한 쪽이 거의 항상 맞다.

## Select: API는 하나, 엔진은 둘

기존 \`Select\`가 Radix Select(본질이 단일 값) 위에 multi-select를 흉내 내고 있었다. \`value\`를 빈 문자열로 비우고 자동 닫힘을 가로채는 우회라, **컴포넌트가 정작 뭐가 선택됐는지를 몰랐다.** 진짜 선택 집합도, 체크박스도, \`aria-checked\`도 없었다.

single과 multi를 한 엔진에 욱여넣는 대신 **API는 통합하되 내부를 두 엔진으로 갈랐다.** single은 Radix Select를 그대로 래핑해 폼 바인딩을 공짜로 가져오고, multi는 Radix Popover 위에 선택 모델·키보드 내비·체크박스를 직접 짰다. 소비자가 쓰는 JSX는 양쪽이 똑같다.

\`\`\`ts
type SingleProps = Common & { multiple?: false; value?: string;   onValueChange?: (v: string)   => void }
type MultiProps  = Common & { multiple: true;  value: string[];  onValueChange:  (v: string[]) => void }
export type SelectV2Props = SingleProps | MultiProps
\`\`\`

\`multiple\`을 주면 컴파일러가 \`string[]\`과 배열 콜백을 요구하고, 빼면 \`string\`을 강제한다. **잘못 쓰면 런타임 가기 전에 타입 체크에서 막힌다.**

## 아이콘 코드젠, 그리고 빌드가 두 번 죽은 얘기

아이콘이 수작업이었다. 디자이너가 Figma에서 뽑은 SVG를 누가 손으로 컴포넌트로 옮기고 export에 한 줄씩 추가하는 식인데, 160개쯤 되니 사고 대기였다. 코드젠으로 뒤집었다 — 폴더에 SVG를 드롭하고 \`generate\`를 돌리면 디렉토리를 스캔해 export 묶음을 통째로 다시 만든다. **폴더가 곧 진실.**

근데 빌드가 두 번 죽었다.

1. **\`?react\` 쿼리.** export가 \`'...svg?react'\`로 import하는데, Storybook(Vite)에선 멀쩡하고 라이브러리 빌드(Rollup)만 죽었다. \`?react\`는 vite-plugin-svgr 규칙이라 Vite에서만 의미가 있는 거였다. 소스를 두 벌로 가르는 대신, 쿼리를 떼고 실제 경로로 넘기는 작은 Rollup 플러그인을 끼웠다.
2. **SVGO \`removeViewBox\`.** 빌드는 통과하는데 화면에서 아이콘이 잘렸다. \`width\`/\`height\`랑 \`viewBox\`가 둘 다 있으면 \`removeViewBox\`가 viewBox를 지워, 좌표계가 사라져 CSS 스케일이 path를 잘라먹었다. Figma를 viewBox-only로 다시 뽑고, 빌드 SVGO에 \`removeViewBox:false\`로 못을 박았다.

\`?react\` 쪽은 타입 체크만으론 안 잡혀서(타입 선언이 그냥 통과시킨다), **push 전에 type-check + 실제 Rollup 빌드를 강제하는 게이트**를 걸었다. 같은 부류 실패는 이제 내 로컬을 못 벗어난다.

## 회고

컴포넌트 하나 잘 만드는 거랑, 다음 사람이 잘 만들 수밖에 없게 깔아두는 건 확실히 다른 일이더라. 여기서 컨벤션 하나 잘못 정하면 그게 소비 프로젝트마다 그대로 복제되니까, 사소한 것도 함부로 못 정했다. 설정상 모든 PR의 코드오너라 리뷰·머지를 도맡고 릴리스도 직접 cut했다.
`.trim(),
  },
  {
    slug: 'work-ml',
    title: '관제사가 코드 없이 탐지 모델을 학습시키는 마법사',
    company: '@Zipida · KISTI',
    period: '2021 — 2022',
    role: 'Frontend · 어드민 단독',
    summary: '학습은 몇 분에서 몇 시간씩 걸리는 비동기 작업이다. 그래서 화면의 본질은 “지금 어느 단계가 돌고 있나”를 보여주는 상태머신이었다.',
    tags: ['상태머신', 'URL-as-state', 'immer', 'NestJS · Celery', 'Elasticsearch'],
    hero: { src: kistiImg, alt: 'KISTI AI 관제 — 모델 학습 화면' },
    metrics: [
      { label: '모델 학습', after: '5단계 GUI', gain: '코드 없이 1건 학습·배포' },
      { label: '파이프라인 상태', after: '5×4', unit: '추적', gain: '멈춤/진행 구분' },
      { label: 'ES 기간 필터 버그', after: 'should→must', gain: '무시되던 필터 정상화' },
    ],
    body: `
## 문제

보안 관제 담당자가 네트워크 공격을 자동 탐지하는 머신러닝 모델을, 코드 한 줄 안 쓰고 GUI만으로 학습시키는 화면이다. 데이터 선택 → 특징 추출 → 알고리즘 학습 → 테스트 → 요약, **5단계 마법사.** 화면으로 보면 폼이지만 실제로는 ML 파이프라인 하나를 통째로 운영하는 콘솔이었다.

마법사는 단순한 듯 안 단순하다. 사용자는 5단계를 앞뒤로 들락거리고, 새로고침·뒤로가기·딥링크도 쓴다. 여기서 두 가지가 동시에 깨지기 쉽다. **뒤로 갔을 때 입력이 남아 있어야** 하고, 반대로 **앞 단계를 바꾸면 그에 의존하던 뒤 단계는 무효가 돼야** 한다. Step1의 데이터를 갈아끼웠는데 Step2·3이 옛 데이터 기준으로 남으면, 몇 시간짜리 워커 작업이 엉뚱한 조합으로 낭비된다.

## 결정 — 단계는 URL, 데이터는 리듀서

보통은 \`currentStep\`을 React state로 든다. 쉬우니까. 근데 그러면 새로고침하면 날아가고 뒤로가기는 마법사를 빠져나가버린다. 그래서 **현재 단계는 URL(\`router.query.step\`)을 단일 소스로** 삼았다. 단계 전환은 곧 \`router.push\`. 새로고침·뒤로가기·딥링크가 공짜로 동작했다.

입력 데이터는 URL에 다 못 넣으니 \`useImmerReducer\`로 들었다. 단계별 객체가 깊게 중첩돼(\`step3.algorithmOptions.explorationDatas.cvFold\`) 불변 갱신을 손으로 펼치면 지옥이라, immer에 맡겼다. 상태머신의 핵심 규칙은 **“리소스 id가 생기면 그 단계가 완료된다”** — \`collectId\`가 세팅되는 순간 \`step1.completed\`를 켠다. 그리고 **“앞이 바뀌면 뒤를 비운다”를 UI가 아니라 리듀서에 명시**했다. 한 번 데인 뒤 넣은 규칙이다.

## 비동기 학습 추적

모델 1건은 crawl → feature → normalize → train → test 5단계를 거치고, 각 단계가 \`S/R/C/E\`(대기/실행중/완료/에러) 상태를 갖는다. 백엔드가 단계 상태를 한 글자씩 이어붙인 progress 문자열로 내려주면, 화면은 그걸 폴링해 다시 그렸다. 재밌었던 건 **이 상태 조합만으로 어느 워커가 일하는지까지 읽어낸다**는 거다 — crawl/feature는 Java 워커, normalize/train/test는 Python 워커인 이종 분산 구조라, 대시보드 큐는 상태 조합만 보고 어느 큐에 일이 쌓였는지 분류했다. NestJS → Python 워커 다리는 Celery(RabbitMQ).

ES \`bool\`에서도 호되게 당했다. 기간(range) 조건을 줘도 필터가 안 먹고 전체가 나왔는데, range를 \`should\`에 넣고 있었던 거다. **\`must\`가 하나라도 있으면 \`should\`는 “있으면 가점, 없어도 통과”라 조건이 아니게 된다.** \`must\`로 옮기니 바로 됐다.

## 회고

이 프로젝트로 “GUI로 비동기 파이프라인을 운영한다”는 게 뭔지 몸으로 알았다. 화면은 폼처럼 생겼지만 본질은 **백엔드에서 길게 도는 작업의 상태를 정직하게 비추는 거울.** 그래서 제일 신경 쓴 건 예쁜 UI가 아니라 “지금 어디까지 됐고, 뭐가 빠졌고, 어디서 깨졌나”를 거짓 없이 보여주는 거였다.

하나 더. 시작할 땐 ML 지식이 사실상 없었다. 이 화면을 제대로 짜려면 각 단계가 뭘 하는지를 알아야 폼 하나도 똑바로 못 만들어서, 백엔드·데이터 쪽 어깨너머로 파이프라인이 어떻게 도는지를 계속 주워들었다. 프론트 화면 하나 만들겠다고 붙었다가 ML 파이프라인의 생김새를 덤으로 배운 셈이다.
`.trim(),
  },
  {
    slug: 'work-blackhole',
    title: '인터스텔라에 빠져 만든 블랙홀, 결국 수학을 배웠다',
    company: '개인 프로젝트',
    period: '2025 — 2026',
    role: '단독 · 셰이더/파이프라인',
    summary: '물리는 포기하고 룩만 챙겼는데, 좌표 수학에서 한참 데였다. 라이브러리 없이 raw WebGL 1.0 + GLSL.',
    tags: ['WebGL', 'GLSL 셰이더', 'Canvas 2D', '좌표 수학'],
    repo: { label: 'GitHub · H8njo/webgl-black-hole', href: 'https://github.com/H8njo/webgl-black-hole' },
    body: `
## 시작

인터스텔라 가르강튀아에서 빛이 왜곡되는 효과를 보고 “저거 어떻게 만들지” 하다가 시작했다. 처음엔 빛을 진짜 휘게 만들어야 하는 줄 알고 물리적으로 정확한 글을 몇 개 읽었는데, 솔직히 몇 문단 못 가서 멈췄다. **논문 재현이 목적은 아니었으니까.** GLSL 셰이더와 WebGL 파이프라인을 처음부터 끝까지 한 번 짜보고 싶었던 사이드 프로젝트다. 물리적으로 정확한 빛의 경로가 아니라 화면 좌표에서 그럴듯하게 빨려드는 룩이면 됐다.

## 트레이드오프 — 별판은 Canvas 2D로

별 배경을 어떻게 그릴지가 첫 갈림길이었다. 별 사진을 찾아다녔는데, 칠흑 같은 어둠에 별만 박힌 그림이 거의 없었다. 배경이 환하거나 성운 색이 진해 왜곡을 먹이면 별이 휘는 게 아니라 배경 전체가 출렁댔다. 정지 사진을 붙이니 블랙홀만 혼자 도는 게 더 어색했다.

그래서 별을 직접 그렸다. 정석대로면 별도 WebGL로 그려 한 패스에 합성하는 게 맞다. 그런데 수천 개 별의 크기 분포·색 온도·후광을 빠르게 만지고 싶었고, 그건 Canvas 2D가 압도적으로 편하다. 그래서 **별판은 Canvas 2D로 그리고, 그 캔버스를 매 프레임 통째로 WebGL 텍스처로 업로드**하는 2-레이어 구조로 갔다. \`texImage2D(..., canvas)\`는 매 프레임 CPU→GPU로 픽셀을 미는 무거운 경로다. 모르고 고른 게 아니라, 데모 해상도에서 60fps가 무난히 나오는 선까지는 개발 속도를 사는 게 맞다고 봤다(트레이드오프는 문서에 적어뒀다).

## 접근

셰이더 한 장에서 전체 파이프라인을 직접 짰다. 핵심은 거리에서 당김을 뽑고, 그 당김을 회전각으로 써서 배경 좌표를 중심 주위로 돌리는 몇 줄이다.

\`\`\`glsl
// 역제곱 당김 — +0.001 epsilon이 없으면 중심(dist=0)에서 폭발한다.
float pull = (u_mass / (dist * dist + 0.001)) * falloffFactor;
// 의도적으로 "틀린" 행렬: 순수 회전이면 sin/cos가 같은 각을 써야 하지만
// cos(angle*PI)와 sin(angle*0.5)로 다른 각을 먹여 전단(shear)을 섞었다.
vec2 r = rotate(mouseUv, uv, pull);
\`\`\`

순수 회전이 아니라 비틀림이 섞이면서 더 “빨려드는” 느낌이 났다. **물리 정확성보다 룩을 택한 결정**이라, 코드 옆에 “왜 틀리게 짰는지” 주석을 박아뒀다. 안 그러면 나중에 내가 보고 “버그네” 하고 고쳐버린다(실제로 한 번 그럴 뻔했다).

## 가장 오래 잡은 건 셰이더가 아니라 좌표계

레티나(\`devicePixelRatio > 1\`)에서 왜곡 중심과 별판이 어긋났다. 두 레이어가 해상도 정의를 다르게 잡고 있었던 거다 — 별판은 \`canvas.width = rect.width * dpr\`, WebGL은 \`gl.drawingBufferWidth\`. 한 캔버스를 다른 캔버스의 텍스처로 쓰는데 두 좌표계가 따로 노니, 텍스처 매핑·마우스 좌표·왜곡 중심이 전부 미세하게 틀어졌다. **이렇게 미세하게 틀어진 게 제일 잡기 어렵더라.** 해결은 싱거웠다 — 양쪽 다 \`getBoundingClientRect()\` 기반 논리 해상도로 통일.

## 회고

셰이더는 “정확”보다 “그럴듯”이 먼저인 영역이 있더라. 일부러 틀리게 짠 회전 행렬이 더 좋은 결과를 냈으니까. 다시 만든다면 매 프레임 \`texImage2D\` 재업로드는 손볼 거다 — 별을 더 늘리거나 고해상도로 가면 거기가 먼저 막힌다. 사이드 프로젝트라 “60fps 나오면 됐다” 선에서 멈췄는데, **그 선을 어디에 그을지 스스로 정하는 것도 결국 엔지니어링**이라고 생각한다.
`.trim(),
  },
];

export const postBySlug = (slug: string) => posts.find((p) => p.slug === slug);
